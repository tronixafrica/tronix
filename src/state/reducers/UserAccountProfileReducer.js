import { Action } from "history"

export const initialProfileState = {
    user: {}
}

const UserAccountProfileReducer = (state, action) => {
    switch(action.type) {
        default:
            return state
    }
}

export default UserAccountProfileReducer